<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.wbutcollegeselector">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminLogin" />
        <activity android:name=".AdminHome" />
        <activity android:name=".StudentLogin" />
        <activity android:name=".StudentHome" />
        <activity android:name=".StudentSignup" />
        <activity android:name=".AdminSignup" />
        <activity android:name=".Add_Update_College" />
        <activity android:name=".Add_College" />
        <activity android:name=".UpdateCollegeDetails" />
        <activity
            android:name=".FillCollegeChoice"
            android:label="@string/title_activity_fill_college_choice"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>